<Window x:Class="ExtendedPenTool.Views.LayerPropertiesView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:ExtendedPenTool"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:models="clr-namespace:ExtendedPenTool.Models"
        mc:Ignorable="d"
        Title="{x:Static local:Texts.LayerPropertyTitle}" Height="150" Width="300"
        WindowStartupLocation="CenterOwner"
        ShowInTaskbar="False"
        ResizeMode="NoResize">
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Horizontal">
            <Label Content="{x:Static local:Texts.LayerOpacityLabel}" VerticalAlignment="Center"/>
            <Slider Value="{Binding Opacity, Mode=TwoWay}" Minimum="0" Maximum="1" Width="150" VerticalAlignment="Center" Margin="5,0"/>
            <TextBlock Text="{Binding Opacity, StringFormat=P0}" VerticalAlignment="Center" MinWidth="30"/>
        </StackPanel>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="OK" Width="75" Margin="5" IsDefault="True" Click="OkButton_Click"/>
            <Button Content="{x:Static local:Texts.Cancel}" Width="75" Margin="5" IsCancel="True" Click="CancelButton_Click"/>
        </StackPanel>
    </Grid>
</Window>
