<Window x:Class="ExtendedPenTool.Views.PenToolView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:ExtendedPenTool.ViewModels"
        xmlns:local="clr-namespace:ExtendedPenTool"
        xmlns:brush="clr-namespace:ExtendedPenTool.Brush"
        xmlns:models="clr-namespace:ExtendedPenTool.Models"
        xmlns:enums="clr-namespace:ExtendedPenTool.Enums"
        xmlns:settings="clr-namespace:ExtendedPenTool.Settings"
        xmlns:converters="clr-namespace:ExtendedPenTool.Converters"
        xmlns:views="clr-namespace:ExtendedPenTool.Views"
        xmlns:controls="clr-namespace:YukkuriMovieMaker.Controls;assembly=YukkuriMovieMaker.Controls"
        xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
        mc:Ignorable="d"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        Title="{x:Static local:Texts.PluginName}"
        d:DataContext="{d:DesignInstance Type={x:Type vm:PenToolViewModel}}">
    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
        <converters:EnumToBooleanConverter x:Key="EnumToBooleanConverter"/>
        <converters:SelectedToColorConverter x:Key="SelectedToColorConverter"/>
        <converters:HistoryKindToStringConverter x:Key="HistoryKindToStringConverter"/>

        <Style TargetType="ListViewItem">
            <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
            <Setter Property="Foreground" Value="{DynamicResource {x:Static SystemColors.ControlTextBrushKey}}"/>
            <EventSetter Event="Drop" Handler="ListViewItem_Drop"/>
            <EventSetter Event="DragOver" Handler="ListViewItem_DragOver"/>
            <EventSetter Event="DragLeave" Handler="ListViewItem_DragLeave"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ListViewItem">
                        <Grid SnapsToDevicePixels="true">
                            <Border x:Name="HoverOverlay" Background="{DynamicResource {x:Static SystemColors.HighlightBrushKey}}" Opacity="0" IsHitTestVisible="False"/>
                            <Border x:Name="SelectionOverlay" Background="{DynamicResource {x:Static SystemColors.HighlightBrushKey}}" Opacity="0" IsHitTestVisible="False"/>
                            <Border x:Name="FocusOverlay" BorderBrush="{DynamicResource {x:Static SystemColors.HighlightBrushKey}}" BorderThickness="1" Opacity="0" IsHitTestVisible="False"/>
                            <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}" VerticalAlignment="{TemplateBinding VerticalContentAlignment}" SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}"/>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="HoverOverlay" Property="Opacity" Value="0.12"/>
                            </Trigger>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="SelectionOverlay" Property="Opacity" Value="0.35"/>
                            </Trigger>
                            <MultiTrigger>
                                <MultiTrigger.Conditions>
                                    <Condition Property="IsSelected" Value="True"/>
                                    <Condition Property="IsMouseOver" Value="True"/>
                                </MultiTrigger.Conditions>
                                <Setter TargetName="SelectionOverlay" Property="Opacity" Value="0.5"/>
                            </MultiTrigger>
                            <Trigger Property="IsFocused" Value="True">
                                <Setter TargetName="FocusOverlay" Property="Opacity" Value="0.8"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="PanelHeaderStyle" TargetType="Border">
            <Setter Property="Background" Value="{DynamicResource {x:Static SystemColors.ActiveCaptionBrushKey}}"/>
            <Setter Property="Cursor" Value="SizeAll"/>
            <Setter Property="Height" Value="24"/>
        </Style>
        <Style x:Key="PanelTitleStyle" TargetType="TextBlock">
            <Setter Property="Foreground" Value="{DynamicResource {x:Static SystemColors.ActiveCaptionTextBrushKey}}"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="Margin" Value="8,0"/>
        </Style>

        <Style x:Key="DraggablePanelStyle" TargetType="ContentControl">
            <Setter Property="BorderBrush" Value="{DynamicResource {x:Static SystemColors.ActiveBorderBrushKey}}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Background" Value="{DynamicResource {x:Static SystemColors.ControlBrushKey}}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ContentControl">
                        <Grid>
                            <Border BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" Background="{TemplateBinding Background}">
                                <Grid>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition/>
                                    </Grid.RowDefinitions>
                                    <Border x:Name="Header" Grid.Row="0" Style="{StaticResource PanelHeaderStyle}">
                                        <TextBlock Text="{TemplateBinding Tag}" Style="{StaticResource PanelTitleStyle}"/>
                                    </Border>
                                    <ContentPresenter Grid.Row="1"/>
                                </Grid>
                            </Border>
                            <Thumb x:Name="ResizeTop" Cursor="SizeNS" Height="4" VerticalAlignment="Top" HorizontalAlignment="Stretch" DragCompleted="ResizeThumb_DragCompleted"/>
                            <Thumb x:Name="ResizeBottom" Cursor="SizeNS" Height="4" VerticalAlignment="Bottom" HorizontalAlignment="Stretch" DragCompleted="ResizeThumb_DragCompleted"/>
                            <Thumb x:Name="ResizeLeft" Cursor="SizeWE" Width="4" HorizontalAlignment="Left" VerticalAlignment="Stretch" DragCompleted="ResizeThumb_DragCompleted"/>
                            <Thumb x:Name="ResizeRight" Cursor="SizeWE" Width="4" HorizontalAlignment="Right" VerticalAlignment="Stretch" DragCompleted="ResizeThumb_DragCompleted"/>
                            <Thumb x:Name="ResizeTopLeft" Cursor="SizeNWSE" Width="8" Height="8" VerticalAlignment="Top" HorizontalAlignment="Left" DragCompleted="ResizeThumb_DragCompleted"/>
                            <Thumb x:Name="ResizeTopRight" Cursor="SizeNESW" Width="8" Height="8" VerticalAlignment="Top" HorizontalAlignment="Right" DragCompleted="ResizeThumb_DragCompleted"/>
                            <Thumb x:Name="ResizeBottomLeft" Cursor="SizeNESW" Width="8" Height="8" VerticalAlignment="Bottom" HorizontalAlignment="Left" DragCompleted="ResizeThumb_DragCompleted"/>
                            <Thumb x:Name="ResizeBottomRight" Cursor="SizeNWSE" Width="8" Height="8" VerticalAlignment="Bottom" HorizontalAlignment="Right" DragCompleted="ResizeThumb_DragCompleted"/>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="IconPathStyle" TargetType="Path">
            <Setter Property="Fill" Value="{DynamicResource {x:Static SystemColors.ControlTextBrushKey}}"/>
            <Setter Property="Width" Value="16"/>
            <Setter Property="Height" Value="16"/>
            <Setter Property="Stretch" Value="Uniform"/>
            <Setter Property="HorizontalAlignment" Value="Center"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
        </Style>

        <Style TargetType="{x:Type ButtonBase}" x:Key="ToolButtonBaseStyle">
            <Setter Property="Width" Value="32"/>
            <Setter Property="Height" Value="32"/>
            <Setter Property="Padding" Value="4"/>
            <Setter Property="Margin" Value="2"/>
            <Setter Property="Foreground" Value="{DynamicResource {x:Static SystemColors.ControlTextBrushKey}}"/>
        </Style>

        <ControlTemplate x:Key="RichButtonTemplate" TargetType="{x:Type ButtonBase}">
            <Border x:Name="border" Background="Transparent" BorderBrush="Transparent" BorderThickness="1" Padding="{TemplateBinding Padding}">
                <ContentPresenter x:Name="contentPresenter" HorizontalAlignment="Center" VerticalAlignment="Center" Opacity="0.5"/>
            </Border>
            <ControlTemplate.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Foreground" Value="{DynamicResource {x:Static SystemColors.HighlightBrushKey}}"/>
                    <Setter TargetName="contentPresenter" Property="Opacity" Value="1.0"/>
                </Trigger>
                <Trigger Property="IsPressed" Value="True">
                    <Setter Property="Foreground" Value="{DynamicResource {x:Static SystemColors.HotTrackBrushKey}}"/>
                    <Setter TargetName="border" Property="BorderBrush" Value="{DynamicResource {x:Static SystemColors.HighlightBrushKey}}"/>
                    <Setter TargetName="contentPresenter" Property="Opacity" Value="1.0"/>
                </Trigger>
                <Trigger Property="IsEnabled" Value="False">
                    <Setter Property="Foreground" Value="{DynamicResource {x:Static SystemColors.GrayTextBrushKey}}"/>
                    <Setter TargetName="contentPresenter" Property="Opacity" Value="0.3"/>
                </Trigger>
            </ControlTemplate.Triggers>
        </ControlTemplate>

        <ControlTemplate x:Key="RichRadioButtonTemplate" TargetType="{x:Type RadioButton}">
            <Border x:Name="border" Background="Transparent" BorderBrush="Transparent" BorderThickness="1" Padding="{TemplateBinding Padding}">
                <ContentPresenter x:Name="contentPresenter" HorizontalAlignment="Center" VerticalAlignment="Center" Opacity="0.5"/>
            </Border>
            <ControlTemplate.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Foreground" Value="{DynamicResource {x:Static SystemColors.HighlightBrushKey}}"/>
                    <Setter TargetName="contentPresenter" Property="Opacity" Value="1.0"/>
                </Trigger>
                <Trigger Property="IsPressed" Value="True">
                    <Setter Property="Foreground" Value="{DynamicResource {x:Static SystemColors.HotTrackBrushKey}}"/>
                </Trigger>
                <Trigger Property="IsChecked" Value="True">
                    <Setter Property="Foreground" Value="{DynamicResource {x:Static SystemColors.HighlightBrushKey}}"/>
                    <Setter TargetName="border" Property="BorderBrush" Value="{DynamicResource {x:Static SystemColors.HighlightBrushKey}}"/>
                    <Setter TargetName="contentPresenter" Property="Opacity" Value="1.0"/>
                </Trigger>
                <Trigger Property="IsEnabled" Value="False">
                    <Setter Property="Foreground" Value="{DynamicResource {x:Static SystemColors.GrayTextBrushKey}}"/>
                    <Setter TargetName="contentPresenter" Property="Opacity" Value="0.3"/>
                </Trigger>
            </ControlTemplate.Triggers>
        </ControlTemplate>

        <Style TargetType="Button" BasedOn="{StaticResource ToolButtonBaseStyle}">
            <Setter Property="Template" Value="{StaticResource RichButtonTemplate}"/>
        </Style>
        <Style TargetType="RepeatButton" BasedOn="{StaticResource ToolButtonBaseStyle}">
            <Setter Property="Template" Value="{StaticResource RichButtonTemplate}"/>
        </Style>
        <Style TargetType="RadioButton" BasedOn="{StaticResource ToolButtonBaseStyle}">
            <Setter Property="Template" Value="{StaticResource RichRadioButtonTemplate}"/>
        </Style>
        <Style TargetType="controls:DropDownButton" BasedOn="{StaticResource ToolButtonBaseStyle}">
            <Setter Property="Template" Value="{StaticResource RichButtonTemplate}"/>
        </Style>

        <Style x:Key="LayerOperationButtonStyle" TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
            <Setter Property="Template" Value="{StaticResource RichButtonTemplate}"/>
            <Setter Property="Width" Value="Auto"/>
            <Setter Property="Height" Value="Auto"/>
        </Style>

        <Style x:Key="WindowButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Width" Value="46"/>
            <Setter Property="Height" Value="32"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#E5E5E5"/>
                </Trigger>
                <Trigger Property="IsPressed" Value="True">
                    <Setter Property="Background" Value="#CACACB"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="CloseButtonStyle" TargetType="Button" BasedOn="{StaticResource WindowButtonStyle}">
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#E81123"/>
                    <Setter Property="Foreground" Value="White"/>
                </Trigger>
                <Trigger Property="IsPressed" Value="True">
                    <Setter Property="Background" Value="#F1707A"/>
                    <Setter Property="Foreground" Value="White"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="RotationThumbStyle" TargetType="Thumb">
            <Setter Property="Width" Value="20"/>
            <Setter Property="Height" Value="20"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Thumb">
                        <Grid>
                            <Ellipse Fill="White" Stroke="Black" StrokeThickness="1"/>
                            <Path Data="M12,5V1L7,6L12,11V7C15.31,7 18,9.69 18,13C18,16.31 15.31,19 12,19C8.69,19 6,16.31 6,13H4C4,17.42 7.58,21 12,21C16.42,21 20,17.42 20,13C20,8.58 16.42,5 12,5Z" Fill="Black" Stretch="Uniform" Margin="4"/>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Window.InputBindings>
        <KeyBinding Modifiers="Ctrl" Key="Z" Command="{Binding UndoCommand}"/>
        <KeyBinding Modifiers="Ctrl" Key="Y" Command="{Binding RedoCommand}"/>
        <KeyBinding Modifiers="Ctrl+Shift" Key="Z" Command="{Binding RedoCommand}"/>
    </Window.InputBindings>

    <Border BorderBrush="{DynamicResource {x:Static SystemColors.ActiveBorderBrushKey}}" BorderThickness="1" Background="{DynamicResource {x:Static SystemColors.WindowBrushKey}}">
        <DockPanel>
            <Grid DockPanel.Dock="Top" Height="32" Background="Transparent" MouseLeftButtonDown="TitleBar_MouseLeftButtonDown">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBlock Text="{Binding Title, RelativeSource={RelativeSource AncestorType=Window}}" VerticalAlignment="Center" Margin="10,0,0,0" Foreground="{DynamicResource {x:Static SystemColors.WindowTextBrushKey}}" MouseLeftButtonDown="ProtectedArea_MouseLeftButtonDown"/>
                <Menu Grid.Column="1" VerticalAlignment="Center" HorizontalAlignment="Left" Background="Transparent" Margin="10,0,0,0" MouseLeftButtonDown="ProtectedArea_MouseLeftButtonDown">
                    <MenuItem Header="{x:Static local:Texts.MenuView}">
                        <MenuItem Header="{x:Static local:Texts.MenuCanvas}" IsChecked="{Binding IsCanvasVisible, Mode=TwoWay}" IsCheckable="True"/>
                        <MenuItem Header="{x:Static local:Texts.MenuLayer}" IsChecked="{Binding IsLayersVisible, Mode=TwoWay}" IsCheckable="True"/>
                        <MenuItem Header="{x:Static local:Texts.MenuCanvasControl}" IsChecked="{Binding IsCanvasControlPanelVisible, Mode=TwoWay}" IsCheckable="True"/>
                        <MenuItem Header="{x:Static local:Texts.MenuHistory}" IsChecked="{Binding IsHistoryVisible, Mode=TwoWay}" IsCheckable="True"/>
                    </MenuItem>
                    <MenuItem Header="{x:Static local:Texts.MenuSettings}">
                        <MenuItem Header="{x:Static local:Texts.MenuMouseWheelAction}">
                            <MenuItem Header="{x:Static local:Texts.MenuMouseWheelZoom}" IsCheckable="True" IsChecked="{Binding Source={x:Static settings:PenSettings.Default}, Path=MouseWheelAction, Converter={StaticResource EnumToBooleanConverter}, ConverterParameter={x:Static enums:MouseWheelAction.Zoom}}"/>
                            <MenuItem Header="{x:Static local:Texts.MenuMouseWheelPenSize}" IsCheckable="True" IsChecked="{Binding Source={x:Static settings:PenSettings.Default}, Path=MouseWheelAction, Converter={StaticResource EnumToBooleanConverter}, ConverterParameter={x:Static enums:MouseWheelAction.PenSize}}"/>
                        </MenuItem>
                        <MenuItem Header="{x:Static local:Texts.MenuToolbarLayout}">
                            <MenuItem Header="{x:Static local:Texts.DirectionTop}" IsCheckable="True" IsChecked="{Binding ToolbarLayout, Converter={StaticResource EnumToBooleanConverter}, ConverterParameter={x:Static enums:ToolbarLayout.Top}}"/>
                            <MenuItem Header="{x:Static local:Texts.DirectionBottom}" IsCheckable="True" IsChecked="{Binding ToolbarLayout, Converter={StaticResource EnumToBooleanConverter}, ConverterParameter={x:Static enums:ToolbarLayout.Bottom}}"/>
                            <MenuItem Header="{x:Static local:Texts.DirectionLeft}" IsCheckable="True" IsChecked="{Binding ToolbarLayout, Converter={StaticResource EnumToBooleanConverter}, ConverterParameter={x:Static enums:ToolbarLayout.Left}}"/>
                            <MenuItem Header="{x:Static local:Texts.DirectionRight}" IsCheckable="True" IsChecked="{Binding ToolbarLayout, Converter={StaticResource EnumToBooleanConverter}, ConverterParameter={x:Static enums:ToolbarLayout.Right}}"/>
                        </MenuItem>
                    </MenuItem>
                </Menu>
                <StackPanel Grid.Column="2" Orientation="Horizontal">
                    <Button Style="{StaticResource WindowButtonStyle}" Click="MinimizeButton_Click">
                        <Path Data="M0 5 H10" Stroke="{Binding Foreground, RelativeSource={RelativeSource AncestorType=Button}}" StrokeThickness="1.5" Width="10" Height="10"/>
                    </Button>
                    <Button Style="{StaticResource CloseButtonStyle}" Click="CloseButton_Click">
                        <Path Data="M0 0 L10 10 M0 10 L10 0" Stroke="{Binding Foreground, RelativeSource={RelativeSource AncestorType=Button}}" StrokeThickness="1.5" Width="10" Height="10"/>
                    </Button>
                </StackPanel>
            </Grid>

            <Border DockPanel.Dock="Bottom" BorderBrush="{DynamicResource {x:Static SystemColors.ControlDarkBrushKey}}" BorderThickness="0,1,0,0" Padding="4">
                <Button Content="{x:Static local:Texts.Close}" Width="100" HorizontalAlignment="Right" Click="CloseButton_Click"/>
            </Border>

            <DockPanel>
                <Border BorderBrush="{DynamicResource {x:Static SystemColors.ControlDarkBrushKey}}" Padding="4">
                    <Border.Style>
                        <Style TargetType="Border">
                            <Setter Property="DockPanel.Dock" Value="Top"/>
                            <Setter Property="BorderThickness" Value="0,0,0,1"/>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding ToolbarLayout}" Value="{x:Static enums:ToolbarLayout.Bottom}">
                                    <Setter Property="DockPanel.Dock" Value="Bottom"/>
                                    <Setter Property="BorderThickness" Value="0,1,0,0"/>
                                </DataTrigger>
                                <DataTrigger Binding="{Binding ToolbarLayout}" Value="{x:Static enums:ToolbarLayout.Left}">
                                    <Setter Property="DockPanel.Dock" Value="Left"/>
                                    <Setter Property="BorderThickness" Value="0,0,1,0"/>
                                </DataTrigger>
                                <DataTrigger Binding="{Binding ToolbarLayout}" Value="{x:Static enums:ToolbarLayout.Right}">
                                    <Setter Property="DockPanel.Dock" Value="Right"/>
                                    <Setter Property="BorderThickness" Value="1,0,0,0"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </Border.Style>
                    <StackPanel>
                        <StackPanel.Style>
                            <Style TargetType="StackPanel">
                                <Setter Property="Orientation" Value="Horizontal"/>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding ToolbarLayout}" Value="{x:Static enums:ToolbarLayout.Left}">
                                        <Setter Property="Orientation" Value="Vertical"/>
                                    </DataTrigger>
                                    <DataTrigger Binding="{Binding ToolbarLayout}" Value="{x:Static enums:ToolbarLayout.Right}">
                                        <Setter Property="Orientation" Value="Vertical"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </StackPanel.Style>

                        <RadioButton ToolTip="{x:Static local:Texts.TooltipPen}" IsChecked="{Binding SelectedBrushType, Converter={StaticResource EnumToBooleanConverter}, ConverterParameter={x:Static brush:BrushType.Pen}}" GroupName="ToolSelection">
                            <Path Style="{StaticResource IconPathStyle}" Data="M20.71,7.04C21.1,6.65 21.1,6 20.71,5.63L18.37,3.29C18,2.9 17.35,2.9 16.96,3.29L15.12,5.12L18.87,8.87M3,17.25V21H6.75L17.81,9.93L14.06,6.18L3,17.25Z"/>
                            <RadioButton.ContextMenu>
                                <ContextMenu>
                                    <MenuItem Header="{x:Static local:Texts.BrushPressure}" IsCheckable="True" IsChecked="{Binding Source={x:Static settings:PenSettings.Default}, Path=PenStyle.IsPressure, Mode=TwoWay}"/>
                                </ContextMenu>
                            </RadioButton.ContextMenu>
                        </RadioButton>
                        <RadioButton ToolTip="{x:Static local:Texts.TooltipHighlighter}" IsChecked="{Binding SelectedBrushType, Converter={StaticResource EnumToBooleanConverter}, ConverterParameter={x:Static brush:BrushType.Highlighter}}" GroupName="ToolSelection">
                            <Path Style="{StaticResource IconPathStyle}" Data="M18.5,1.71L22.29,5.5L20.29,7.5L16.5,3.71L18.5,1.71M2,17V21H6L15.88,11.12L11.88,7.12L2,17Z"/>
                            <RadioButton.ContextMenu>
                                <ContextMenu>
                                    <MenuItem Header="{x:Static local:Texts.BrushPressure}" IsCheckable="True" IsChecked="{Binding Source={x:Static settings:PenSettings.Default}, Path=HighlighterStyle.IsPressure, Mode=TwoWay}"/>
                                </ContextMenu>
                            </RadioButton.ContextMenu>
                        </RadioButton>
                        <RadioButton ToolTip="{x:Static local:Texts.TooltipPencil}" IsChecked="{Binding SelectedBrushType, Converter={StaticResource EnumToBooleanConverter}, ConverterParameter={x:Static brush:BrushType.Pencil}}" GroupName="ToolSelection">
                            <Path Style="{StaticResource IconPathStyle}" Data="M14.06,9L15,9.94L5.92,19H5V18.08L14.06,9M17.66,3C17.41,3 17.15,3.1 16.96,3.29L15.13,5.12L18.88,8.87L20.71,7.04C21.1,6.65 21.1,6 20.71,5.63L18.37,3.29C18.17,3.09 17.92,3 17.66,3M14.06,6.19L3,17.25V21H6.75L17.81,9.94L14.06,6.19Z"/>
                            <RadioButton.ContextMenu>
                                <ContextMenu>
                                    <MenuItem Header="{x:Static local:Texts.BrushPressure}" IsCheckable="True" IsChecked="{Binding Source={x:Static settings:PenSettings.Default}, Path=PencilStyle.IsPressure, Mode=TwoWay}"/>
                                </ContextMenu>
                            </RadioButton.ContextMenu>
                        </RadioButton>
                        <RadioButton ToolTip="{x:Static local:Texts.TooltipEraser}" IsChecked="{Binding SelectedBrushType, Converter={StaticResource EnumToBooleanConverter}, ConverterParameter={x:Static brush:BrushType.Eraser}}" GroupName="ToolSelection">
                            <Path Style="{StaticResource IconPathStyle}" Data="M16.24,3.56L21.19,8.5C21.97,9.29 21.97,10.55 21.19,11.34L12,20.53C10.44,22.09 7.91,22.09 6.34,20.53L2.81,17C2.03,16.21 2.03,14.95 2.81,14.16L13.41,3.56C14.2,2.78 15.46,2.78 16.24,3.56M4.22,15.58L7.76,19.11C8.54,19.9 9.8,19.9 10.59,19.11L14.12,15.58L9.17,10.63L4.22,15.58Z"/>
                            <RadioButton.ContextMenu>
                                <ContextMenu>
                                    <MenuItem Header="{x:Static local:Texts.EraserPointMode}" IsChecked="{Binding Source={x:Static settings:PenSettings.Default}, Path=EraserStyle.Mode, Converter={StaticResource EnumToBooleanConverter}, ConverterParameter={x:Static enums:EraserMode.Point}}" Command="{Binding SwitchEraserModeCommand}" CommandParameter="{x:Static enums:EraserMode.Point}"/>
                                    <MenuItem Header="{x:Static local:Texts.EraserStrokeMode}" IsChecked="{Binding Source={x:Static settings:PenSettings.Default}, Path=EraserStyle.Mode, Converter={StaticResource EnumToBooleanConverter}, ConverterParameter={x:Static enums:EraserMode.Line}}" Command="{Binding SwitchEraserModeCommand}" CommandParameter="{x:Static enums:EraserMode.Line}"/>
                                </ContextMenu>
                            </RadioButton.ContextMenu>
                        </RadioButton>
                        <Separator>
                            <Separator.Style>
                                <Style TargetType="Separator">
                                    <Setter Property="Margin" Value="5,0"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding ToolbarLayout}" Value="{x:Static enums:ToolbarLayout.Left}">
                                            <Setter Property="Margin" Value="0,5"/>
                                        </DataTrigger>
                                        <DataTrigger Binding="{Binding ToolbarLayout}" Value="{x:Static enums:ToolbarLayout.Right}">
                                            <Setter Property="Margin" Value="0,5"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Separator.Style>
                        </Separator>

                        <RadioButton ToolTip="{x:Static local:Texts.TooltipSelect}" IsChecked="{Binding IsSelectMode, Mode=TwoWay}" GroupName="ToolSelection">
                            <Path Style="{StaticResource IconPathStyle}" Data="M15,3H17V5H15M15,19H17V21H15M3,11H5V13H3M3,15H5V17H3M3,7H5V9H3M3,3H5V5H3M7,3H9V5H7M19,15H21V17H19M11,3H13V5H11M19,7H21V9H19M19,11H21V13H19M19,3H21V5H19M7,19H9V21H7M11,19H13V21H11M19,19H21V21H19M3,19H5V21H3Z"/>
                        </RadioButton>
                        <RepeatButton ToolTip="{x:Static local:Texts.TooltipUndo}" Command="{Binding UndoCommand}">
                            <Path Style="{StaticResource IconPathStyle}" Data="M12.5,8C9.85,8 7.45,9.16 5.86,11L3,8V16H11L7.91,12.91C9.13,11.55 10.71,10.75 12.5,10.75C15.61,10.75 18.22,12.89 19.02,15.75L21.5,15C20.42,11.13 16.81,8 12.5,8Z"/>
                        </RepeatButton>
                        <RepeatButton ToolTip="{x:Static local:Texts.TooltipRedo}" Command="{Binding RedoCommand}">
                            <Path Style="{StaticResource IconPathStyle}" Data="M18.4,10.6C16.55,8.99 14.15,8 11.5,8C6.85,8 2.92,11.03 1.54,15.22L3.9,16C4.95,12.81 7.95,10.5 11.5,10.5C13.45,10.5 15.23,11.22 16.62,12.38L13,16H22V7L18.4,10.6Z"/>
                        </RepeatButton>

                        <Separator>
                            <Separator.Style>
                                <Style TargetType="Separator">
                                    <Setter Property="Margin" Value="5,0"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding ToolbarLayout}" Value="{x:Static enums:ToolbarLayout.Left}">
                                            <Setter Property="Margin" Value="0,5"/>
                                        </DataTrigger>
                                        <DataTrigger Binding="{Binding ToolbarLayout}" Value="{x:Static enums:ToolbarLayout.Right}">
                                            <Setter Property="Margin" Value="0,5"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Separator.Style>
                        </Separator>

                        <controls:ColorPicker Value="{Binding StrokeColor, Mode=TwoWay}" IsEnabled="{Binding IsColorPickerEnabled}">
                            <controls:ColorPicker.Style>
                                <Style TargetType="controls:ColorPicker">
                                    <Setter Property="Width" Value="100"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding ToolbarLayout}" Value="{x:Static enums:ToolbarLayout.Left}">
                                            <Setter Property="Width" Value="40"/>
                                            <Setter Property="Height" Value="40"/>
                                        </DataTrigger>
                                        <DataTrigger Binding="{Binding ToolbarLayout}" Value="{x:Static enums:ToolbarLayout.Right}">
                                            <Setter Property="Width" Value="40"/>
                                            <Setter Property="Height" Value="40"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </controls:ColorPicker.Style>
                        </controls:ColorPicker>

                        <Grid>
                            <controls:TextBoxSlider Value="{Binding StrokeThickness, Mode=TwoWay}" Min="0.1" Max="1024" DefaultMin="0.1" DefaultMax="100" Width="150">
                                <controls:TextBoxSlider.Style>
                                    <Style TargetType="controls:TextBoxSlider">
                                        <Setter Property="Visibility" Value="Visible"/>
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding ToolbarLayout}" Value="{x:Static enums:ToolbarLayout.Left}">
                                                <Setter Property="Visibility" Value="Collapsed"/>
                                            </DataTrigger>
                                            <DataTrigger Binding="{Binding ToolbarLayout}" Value="{x:Static enums:ToolbarLayout.Right}">
                                                <Setter Property="Visibility" Value="Collapsed"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </controls:TextBoxSlider.Style>
                            </controls:TextBoxSlider>
                            <StackPanel Height="150">
                                <StackPanel.Style>
                                    <Style TargetType="StackPanel">
                                        <Setter Property="Visibility" Value="Collapsed"/>
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding ToolbarLayout}" Value="{x:Static enums:ToolbarLayout.Left}">
                                                <Setter Property="Visibility" Value="Visible"/>
                                            </DataTrigger>
                                            <DataTrigger Binding="{Binding ToolbarLayout}" Value="{x:Static enums:ToolbarLayout.Right}">
                                                <Setter Property="Visibility" Value="Visible"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </StackPanel.Style>
                                <TextBox Text="{Binding StrokeThickness, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged, StringFormat=F1}" HorizontalContentAlignment="Center" MinWidth="40"/>
                                <Slider Value="{Binding StrokeThickness, Mode=TwoWay}" Minimum="0.1" Maximum="1024" Orientation="Vertical" HorizontalAlignment="Center" Height="120"/>
                            </StackPanel>
                        </Grid>

                        <Separator>
                            <Separator.Style>
                                <Style TargetType="Separator">
                                    <Setter Property="Margin" Value="5,0"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding ToolbarLayout}" Value="{x:Static enums:ToolbarLayout.Left}">
                                            <Setter Property="Margin" Value="0,5"/>
                                        </DataTrigger>
                                        <DataTrigger Binding="{Binding ToolbarLayout}" Value="{x:Static enums:ToolbarLayout.Right}">
                                            <Setter Property="Margin" Value="0,5"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Separator.Style>
                        </Separator>

                        <Button ToolTip="{x:Static local:Texts.TooltipImport}" Command="{Binding ImportIsfCommand}">
                            <Path Style="{StaticResource IconPathStyle}" Data="M5,20H19V18H5M19,9H15V3H9V9H5L12,16L19,9Z"/>
                        </Button>
                        <controls:DropDownButton ToolTip="{x:Static local:Texts.TooltipExport}">
                            <Path Style="{StaticResource IconPathStyle}" Data="M5,20H19V18H5M9,16H15V10H19L12,3L5,10H9V16Z"/>
                            <controls:DropDownButton.ContextMenu>
                                <ContextMenu DataContext="{Binding RelativeSource={RelativeSource Mode=Self},Path=PlacementTarget.DataContext}" d:DataContext="{d:DesignInstance Type={x:Type vm:PenToolViewModel}}">
                                    <MenuItem Header="{x:Static local:Texts.ExportAsImage}" Command="{Binding SaveImageCommand}"/>
                                    <MenuItem Header="{x:Static local:Texts.ExportAsIsf}" Command="{Binding ExportIsfCommand}"/>
                                </ContextMenu>
                            </controls:DropDownButton.ContextMenu>
                        </controls:DropDownButton>
                    </StackPanel>
                </Border>

                <Canvas x:Name="MainCanvas" ClipToBounds="True">
                    <ContentControl x:Name="CanvasPanel" Style="{StaticResource DraggablePanelStyle}" Tag="{x:Static local:Texts.PanelCanvas}" MinWidth="200" MinHeight="150" Visibility="{Binding IsCanvasVisible, Converter={StaticResource BooleanToVisibilityConverter}}">
                        <Canvas x:Name="CanvasViewport" ClipToBounds="True" Background="Transparent" PreviewMouseWheel="CanvasViewport_PreviewMouseWheel" PreviewMouseDown="CanvasViewport_PreviewMouseDown" PreviewMouseMove="CanvasViewport_PreviewMouseMove" MouseUp="CanvasViewport_MouseUp" MouseEnter="CanvasViewport_MouseEnter" MouseLeave="CanvasViewport_MouseLeave">
                            <Grid x:Name="CanvasRoot" Width="{Binding CanvasSize.Width}" Height="{Binding CanvasSize.Height}" RenderTransformOrigin="0.5, 0.5">
                                <Grid.RenderTransform>
                                    <TransformGroup>
                                        <ScaleTransform ScaleX="{Binding CanvasScale}" ScaleY="{Binding CanvasScale}"/>
                                        <RotateTransform Angle="{Binding CanvasAngle}"/>
                                        <TranslateTransform X="{Binding CanvasTranslateX}" Y="{Binding CanvasTranslateY}"/>
                                    </TransformGroup>
                                </Grid.RenderTransform>
                                <Image Source="{Binding Bitmap}" Name="image"/>
                                <InkCanvas Name="inkCanvas" UseCustomCursor="True" DefaultDrawingAttributes="{Binding Pen, Mode=OneWay, UpdateSourceTrigger=PropertyChanged}" EditingMode="{Binding EditingMode}" Background="#01FFFFFF" Strokes="{Binding Strokes}" SelectionChanged="InkCanvas_SelectionChanged"/>
                            </Grid>
                            <Ellipse x:Name="PenPreview" IsHitTestVisible="False" Visibility="Collapsed" StrokeThickness="1" Stroke="Black"/>
                            <Rectangle x:Name="PenPreviewRectangle" IsHitTestVisible="False" Visibility="Collapsed" StrokeThickness="1" Stroke="Black"/>
                        </Canvas>
                    </ContentControl>

                    <ContentControl x:Name="LayersPanel" Style="{StaticResource DraggablePanelStyle}" Tag="{x:Static local:Texts.PanelLayers}" MinWidth="200" MinHeight="200" Visibility="{Binding IsLayersVisible, Converter={StaticResource BooleanToVisibilityConverter}}">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition/>
                                <RowDefinition Height="30"/>
                            </Grid.RowDefinitions>
                            <ListView Grid.Row="0" ItemsSource="{Binding Layers}" SelectedItem="{Binding SelectedLayer}" BorderThickness="0" HorizontalContentAlignment="Stretch" Background="Transparent" AllowDrop="True">
                                <ListView.Resources>
                                    <Style TargetType="Border" x:Key="DragDropIndicator">
                                        <Setter Property="BorderBrush" Value="DodgerBlue"/>
                                        <Setter Property="BorderThickness" Value="0,2,0,0"/>
                                        <Setter Property="Visibility" Value="Collapsed"/>
                                    </Style>
                                </ListView.Resources>
                                <ListView.ItemTemplate>
                                    <DataTemplate DataType="{x:Type models:Layer}">
                                        <StackPanel>
                                            <Border x:Name="DropIndicator" Style="{StaticResource DragDropIndicator}"/>
                                            <Border BorderBrush="{DynamicResource {x:Static SystemColors.ControlDarkBrushKey}}" BorderThickness="0,0,0,1" Padding="5">
                                                <Grid Background="Transparent" PreviewMouseLeftButtonDown="ListViewItem_PreviewMouseLeftButtonDown" MouseMove="ListViewItem_MouseMove">
                                                    <Grid.ColumnDefinitions>
                                                        <ColumnDefinition Width="Auto"/>
                                                        <ColumnDefinition Width="Auto"/>
                                                        <ColumnDefinition/>
                                                        <ColumnDefinition Width="Auto"/>
                                                        <ColumnDefinition Width="Auto"/>
                                                    </Grid.ColumnDefinitions>
                                                    <CheckBox Grid.Column="0" IsChecked="{Binding IsVisible, Mode=OneWay}" VerticalAlignment="Center" Margin="0,0,5,0">
                                                        <CheckBox.Template>
                                                            <ControlTemplate TargetType="CheckBox">
                                                                <Grid>
                                                                    <Path x:Name="EyeIcon" Style="{StaticResource IconPathStyle}" Data="M12,9A3,3 0 0,0 9,12A3,3 0 0,0 12,15A3,3 0 0,0 15,12A3,3 0 0,0 12,9M12,4.5C17,4.5 21.27,7.61 23,12C21.27,16.39 17,19.5 12,19.5C7,19.5 2.73,16.39 1,12C2.73,7.61 7,4.5 12,4.5M12,6.5A5.5,5.5 0 0,0 6.5,12A5.5,5.5 0 0,0 12,17.5A5.5,5.5 0 0,0 17.5,12A5.5,5.5 0 0,0 12,6.5Z" Fill="{Binding Foreground, RelativeSource={RelativeSource AncestorType=ListViewItem}}"/>
                                                                </Grid>
                                                                <ControlTemplate.Triggers>
                                                                    <Trigger Property="IsChecked" Value="False">
                                                                        <Setter TargetName="EyeIcon" Property="Opacity" Value="0.2"/>
                                                                    </Trigger>
                                                                </ControlTemplate.Triggers>
                                                            </ControlTemplate>
                                                        </CheckBox.Template>
                                                    </CheckBox>
                                                    <Border Grid.Column="1" Width="48" Height="27" BorderBrush="{DynamicResource {x:Static SystemColors.ControlDarkBrushKey}}" BorderThickness="1" Margin="5,0" VerticalAlignment="Center">
                                                        <Border.ToolTip>
                                                            <ToolTip HasDropShadow="True">
                                                                <Border BorderBrush="Black" BorderThickness="1">
                                                                    <Grid>
                                                                        <Image Source="{Binding DataContext.Bitmap, RelativeSource={RelativeSource AncestorType=views:PenToolView}}" Stretch="Uniform" Width="256" Height="144"/>
                                                                        <Image Source="{Binding PreviewThumbnail}" Stretch="Uniform" Width="256" Height="144"/>
                                                                    </Grid>
                                                                </Border>
                                                            </ToolTip>
                                                        </Border.ToolTip>
                                                        <Image Source="{Binding Thumbnail}" Stretch="Uniform"/>
                                                    </Border>
                                                    <TextBox Grid.Column="2" Text="{Binding Name}" Margin="5,0" VerticalAlignment="Center" BorderThickness="0" Background="Transparent" Foreground="{Binding Foreground, RelativeSource={RelativeSource AncestorType=ListViewItem}}" MouseDoubleClick="LayerNameTextBox_MouseDoubleClick" IsReadOnly="True" LostFocus="LayerNameTextBox_LostFocus" PreviewKeyDown="LayerNameTextBox_PreviewKeyDown"/>
                                                    <StackPanel Grid.Column="3" Orientation="Horizontal" VerticalAlignment="Center">
                                                        <TextBlock Text="{x:Static local:Texts.LayerLocked}" Visibility="{Binding IsLocked, Converter={StaticResource BooleanToVisibilityConverter}}" VerticalAlignment="Center"/>
                                                        <TextBlock Text="{Binding Opacity, StringFormat=P0}" Margin="5,0" VerticalAlignment="Center"/>
                                                    </StackPanel>
                                                    <Button Grid.Column="4" Content="&#x22EE;" FontFamily="Segoe UI Symbol" Background="Transparent" BorderThickness="0" VerticalAlignment="Center" Command="{Binding DataContext.ShowLayerPropertiesCommand, RelativeSource={RelativeSource AncestorType=ListView}}" CommandParameter="{Binding}">
                                                        <Button.Style>
                                                            <Style TargetType="Button">
                                                                <Setter Property="Foreground" Value="{Binding Foreground, RelativeSource={RelativeSource AncestorType=ListViewItem}}"/>
                                                                <Style.Triggers>
                                                                    <Trigger Property="IsMouseOver" Value="True">
                                                                        <Setter Property="Background" Value="{DynamicResource {x:Static SystemColors.ControlLightBrushKey}}"/>
                                                                    </Trigger>
                                                                </Style.Triggers>
                                                            </Style>
                                                        </Button.Style>
                                                    </Button>
                                                </Grid>
                                            </Border>
                                        </StackPanel>
                                    </DataTemplate>
                                </ListView.ItemTemplate>
                            </ListView>
                            <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Center">
                                <Button Style="{StaticResource LayerOperationButtonStyle}" ToolTip="{x:Static local:Texts.TooltipAddLayer}" Command="{Binding AddLayerCommand}">
                                    <Path Style="{StaticResource IconPathStyle}" Data="M19,13H13V19H11V13H5V11H11V5H13V11H19V13Z"/>
                                </Button>
                                <Button Style="{StaticResource LayerOperationButtonStyle}" ToolTip="{x:Static local:Texts.TooltipRemoveLayer}" Command="{Binding RemoveLayerCommand}">
                                    <Path Style="{StaticResource IconPathStyle}" Data="M19,13H5V11H19V13Z"/>
                                </Button>
                                <Button Style="{StaticResource LayerOperationButtonStyle}" ToolTip="{x:Static local:Texts.TooltipMoveLayerUp}" Command="{Binding MoveLayerUpCommand}">
                                    <Path Style="{StaticResource IconPathStyle}" Data="M7.41,15.41L12,10.83L16.59,15.41L18,14L12,8L6,14L7.41,15.41Z"/>
                                </Button>
                                <Button Style="{StaticResource LayerOperationButtonStyle}" ToolTip="{x:Static local:Texts.TooltipMoveLayerDown}" Command="{Binding MoveLayerDownCommand}">
                                    <Path Style="{StaticResource IconPathStyle}" Data="M7.41,8.58L12,13.17L16.59,8.58L18,10L12,16L6,10L7.41,8.58Z"/>
                                </Button>
                            </StackPanel>
                        </Grid>
                    </ContentControl>

                    <ContentControl x:Name="CanvasControlPanel" Style="{StaticResource DraggablePanelStyle}" Tag="{x:Static local:Texts.PanelCanvasControl}" MinWidth="200" MinHeight="260" Width="200" Height="260" Visibility="{Binding IsCanvasControlPanelVisible, Converter={StaticResource BooleanToVisibilityConverter}}">
                        <views:CanvasControlPanelView/>
                    </ContentControl>

                    <ContentControl x:Name="HistoryPanel" Style="{StaticResource DraggablePanelStyle}" Tag="{x:Static local:Texts.PanelHistory}" MinWidth="150" MinHeight="200" Visibility="{Binding IsHistoryVisible, Converter={StaticResource BooleanToVisibilityConverter}}">
                        <ListView ItemsSource="{Binding History}" SelectedIndex="{Binding CurrentHistoryIndex, Mode=TwoWay}" BorderThickness="0" HorizontalContentAlignment="Stretch" Background="Transparent">
                            <ListView.ItemTemplate>
                                <DataTemplate DataType="{x:Type models:HistoryItem}">
                                    <TextBlock Text="{Binding Kind, Converter={StaticResource HistoryKindToStringConverter}}" Margin="5" Foreground="{Binding Foreground, RelativeSource={RelativeSource AncestorType=ListViewItem}}"/>
                                </DataTemplate>
                            </ListView.ItemTemplate>
                        </ListView>
                    </ContentControl>
                </Canvas>
            </DockPanel>
        </DockPanel>
    </Border>
</Window>
